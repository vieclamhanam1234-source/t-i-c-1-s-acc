{
  "steps": {
    "install": {
      "commands": [
        "cd worker && pip install -r requirements.txt"
      ]
    },
    "playwright": {
      "commands": [
        "cd worker && python -m playwright install --with-deps chromium"
      ]
    }
  }
}
